'on': workflow_dispatch
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Docker Version
        run: docker -v
      - name: Checkout
        uses: actions/checkout@v3
      - name: Dir
        run: ls
      - name: Docker Build
        run: docker build .
